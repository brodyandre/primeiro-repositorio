name: primeira-pipeline
on: 
  push:
    branches: ["main"]
  workflow_dispatch:
jobs:
  primeiro-job:
    name: "Meuprimeiro job"
    runs-on: ubuntu-latest
    steps:
      - name: "minha primeira ação"
        run: echo "Hello World !!!"
      - name: "Segunda ação"
        run:
          echo "Você criou o su primeiro Workflow !!!"
          echo "Parabéns !!!"
      - name:  "Script Python"
        run: 
          variavel = "hello World !!!"
          print(variavel)
        shell: python
